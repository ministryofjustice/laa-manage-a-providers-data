environment: "staging"
replicaCount: 1

ingress:
  enabled: true
  host: "laa-manage-a-providers-data-staging.cloud-platform.service.justice.gov.uk"

whitelist:
  enabled: true

pdb:
  enabled: true
  minAvailable: 1

envVars:
  PDA_USE_MOCK_API:
    value: "True"
  PDA_ENVIRONMENT:
    value: "Mock"
